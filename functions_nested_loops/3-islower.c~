#include "main.h"

/**
 *_islower - function begins
 *Return: end of function
 *@c: int variable name
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
_putchar((zer % 10) + '0');
return (0);
}
else
{
_putchar((one % 10) + '0');
return (0);
}
}
