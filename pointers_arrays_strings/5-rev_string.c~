#include "main.h"
#include <stdio.h>
/**
 *rev_string - function reverses string
 *@s: parameter name
 */
void rev_string(char *s)
{
int i = 0;
int k;
//int j;
while (s[i] != '\0')
{
i++;
}
/**k = i;
for (j = k - 1; j >= 0; j--)
  {
    putchar(s[j]);
    }*/
}
