#include "main.h"

/**
 *print_rev - function to print in reverse
 *@s: parameter name
 */
void print_rev(char *s)
{
 int  i = 0;
 int j;
  for (j = i - 1; j >= 0; j--)
    {
    char s[i];
      _putchar(j);
    }
}
